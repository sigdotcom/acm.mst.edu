"""
Defines the app name for the core app.
"""
# Django
from django.apps import AppConfig


class CoreConfig(AppConfig):
    """
    Defines a global app name for the core app.
    """
    name = 'core'
