"""
Defines the app name for the Events app.
"""
# Django
from django.apps import AppConfig


class EventsConfig(AppConfig):
    """
    Defines a global app name for the Events app.
    """
    name = 'events'
