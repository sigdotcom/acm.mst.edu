"""
Defines the app name for the Payments app.
"""
# Django
from django.apps import AppConfig


class PaymentsConfig(AppConfig):
    """
    Defines a global app name for the Payments app.
    """
    name = 'payments'
